# DisplayLocation.org

Static site supporting the DisplayLocation schema proposal.

## Getting started
- Install dependencies: `npm install`
- Run locally: `npm run dev`
- Build for production: `npm run build`

## Deployment
See [deployment guidance](docs/DEPLOYMENT.md) for how to trigger or retry Cloudflare Pages builds if a release is not initiated automatically.
